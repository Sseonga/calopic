<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="fs.human.calopic.diet.record.mapper.FoodMapper">

    <resultMap id="foodResultMap" type="fs.human.calopic.diet.dto.FoodDto">
        <id property="foodCode" column="TB_CODE" />
        <result property="foodName" column="FOOD_NAME" />
        <result property="foodKcal" column="FOOD_KCAL" />
    </resultMap>

    <select id="searchFoodsByName" resultMap="foodResultMap">
        SELECT
        TB_CODE,
        FOOD_NAME,
        FOOD_KCAL
        FROM
        TB_FOOD
        WHERE
        FOOD_NAME LIKE '%' || #{query} || '%'
        ORDER BY FOOD_NAME
    </select>
</mapper>